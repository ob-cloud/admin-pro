import { getAction, postAction, putAction, deleteAction, httpAction, downloadAction, getRequestUrl } from './action'
import { axios } from './request'

export {
  axios,
  getAction,
  postAction,
  putAction,
  deleteAction,
  httpAction,
  downloadAction,
  getRequestUrl
}
